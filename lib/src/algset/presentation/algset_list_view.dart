import 'package:fluent_ui/fluent_ui.dart';

class AlgsetListView extends StatelessWidget {
  const AlgsetListView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
